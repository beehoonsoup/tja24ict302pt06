<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/public/css/styles.css" rel="stylesheet" type="text/css"/>
    <title>Join Requests</title>
</head>
<body>
    <h1>Join Requests</h1>
    <% if (joinRequests.length === 0) { %>
        <p>No pending join requests.</p>
    <% } else { %>
        <ul>
            <% joinRequests.forEach(request => { %>
                <li>
                    <strong><%= request.UserName %></strong> has requested to join project "<%= request.ProjectName %>".
                    <form action="/accept-join-request" method="post">
                        <input type="hidden" name="requestId" value="<%= request.TeamID %>">
                        <select name="teamRole">
                            <option value="Member">Member</option>
                            <option value="Leader">Leader</option>
                        </select>
                        <button type="submit">Accept</button>
                    </form>
                    <form action="/reject-join-request" method="post">
                        <input type="hidden" name="requestId" value="<%= request.TeamID %>">
                        <button type="submit">Reject</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    <% } %>
</body>
</html>
